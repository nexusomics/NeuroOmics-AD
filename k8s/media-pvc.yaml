apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neuroomics-media
  namespace: neuroomics
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 100Gi
  # storageClassName: efs  # or your RWX provider
